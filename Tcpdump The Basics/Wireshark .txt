

⭐ Wireshark : -

            Wireshark is an open-source, cross-platform network packet analyser tool capable of sniffing and investigating live traffic and 
            inspecting packet captures (PCAP). It is commonly used as one of the best packet analysis tools.

⭐ Objectives :

            Navigate and configure Wireshark
            Inspect packets and discover information from the different layers of TCP/IP
            Apply display filters

⭐ Use Cases :

            Wireshark is one of the most potent traffic analyser tools available in the wild. There are multiple purposes for its use:

                    > Detecting and troubleshooting network problems, such as network load failure points and congestion.
                    > Detecting security anomalies, such as rogue hosts, abnormal port usage, and suspicious traffic.
                    > Investigating and learning protocol details, such as response codes and payload data.


Wireshark has two types of packet colouring methods: temporary rules that are only available during a program session and permanent rules 
that are saved under the preference file (profile) and available for the next program session.

Packet capture (PCAP) is a networking practice involving the interception of data packets travelling over a network. Once the packets 
are captured, they can be stored by IT teams for further analysis. The inspection of these packets allows IT teams to identify issues and 
solve network problems affecting daily operations

⭐ Packet Dissection :
            Packet dissection is also known as protocol dissection, which investigates packet details by decoding available protocols and fields.
            Wireshark supports a long list of protocols for dissection.

Wireshark uses OSI layers to break down packets and for the analysis

We can see seven distinct layers to the packet: frame/packet,source [MAC],source [IP],protocol,protocol errors, application protocol, and application data.

 >>   The Frame (Layer 1):

                    This will show you what frame/packet you are looking at and details specific to the Physical layer of the OSI model.

 >>   Source [MAC] (Layer 2):

                    This will show you the source and destination MAC Addresses; from the Data Link layer of the OSI model.

 >>   Source [IP] (Layer 3):

                    This will show you the source and destination IPv4 Addresses; from the Network layer of the OSI model.

 >>   Protocol (Layer 4):

                    This will show you details of the protocol used (UDP/TCP) and source and destination ports; from the Transport layer of the OSI model.

 >>   Protocol Errors:
 
                    This continuation of the 4th layer shows specific segments from TCP that needed to be reassembled.

 >>   Application Protocol (Layer 5):

                    This will show details specific to the protocol used, such as HTTP, FTP,  and SMB. From the Application layer of the OSI model.

 >>   Application Data:

                    This extension of the 5th layer can show the application-specific data.


TTL : time to live , under layer 3, , TTL stands for Time to Live. It is a field in the header of an IP packet that acts as a hop limit or 
      "self-destruct timer" to prevent packets from circulating endlessly in routing loops.

TCP payload : A TCP payload is the actual application data carried inside a Transmission Control Protocol (TCP) segment. 

E-tag : stand for "Entity tag". ETag (Entity Tag) is an HTTP response header that provides a unique identifier for a specific version of a web resource.
        It is used by browsers and servers to determine whether the resource has changed, enabling efficient caching and reducing unnecessary data transfers.


Wireshark has a powerful filter engine that helps analysts to narrow down the traffic and focus on the event of interest. Wireshark has two types of filtering
approaches: capture and display filters. Capture filters are used for "capturing" only the packets valid for the used filter. Display filters are used for "viewing"
the packets valid for the used filter.
      
      ⭐ Packet Filtering & Display Filters :


        Wireshark provides a powerful filtering engine that helps analysts reduce noise and focus on relevant network traffic.

        There are two types of filters:

                > Capture Filters – Used during packet capture. Only packets matching the filter are captured.
                > Display Filters – Used after capturing packets. All packets are captured, but only matching packets are displayed.


      ⭐ Apply as Filter :

              The quickest way to filter traffic.

                 > Right-click on a field/value and select "Apply as Filter".
                 > Displays only packets containing the selected value.
                 > Useful for investigating a specific IP address, protocol, port, or field value.


      ⭐ Conversation Filter :

              Used to view all packets belonging to a specific communication session.

                  > Filters packets based on source/destination IP addresses and port numbers.
                  > Helps track the complete conversation between two hosts.
                  > Access via Right-click → Conversation Filter.


      ⭐ Colourise Conversation :

              Highlights packets belonging to the same conversation without hiding other packets.

                  > Makes related traffic easier to identify visually.
                  > Does not apply a display filter.
                  > Can be reset using:
                          View → Colourise Conversation → Reset Colourisation


      ⭐ Prepare as Filter :

              Similar to "Apply as Filter" but does not execute immediately.

                  > Generates the filter expression and places it in the filter bar.
                  > Allows combining multiple conditions using AND/OR operators before applying.
                  > Useful for building complex filters.


      ⭐ Apply as Column :

              Adds a selected field as a new column in the packet list pane.

                  > Helps monitor specific values across all packets.
                  > Useful for fields such as:
                          - Source/Destination IP
                          - TCP Stream Index
                          - Hostnames
                          - Response Codes


      ⭐ Follow Stream :

              Reconstructs communication streams and displays data at the application layer.

                  > Supports TCP, UDP, HTTP, and other protocols.
                  > Useful for viewing:
                          - Usernames
                          - Passwords
                          - HTTP Requests/Responses
                          - Application Data

              Packet colours in stream view:

                  > Red  : Client Traffic
                  > Blue : Server Traffic

              Note:
                   Wireshark automatically creates and applies a display filter for the selected stream.


      ⭐ Common Display Filters :

                  > > Filter by Protocol Name

                                  http
                                  dns
                                  arp
                                  ftp
                                  smtp
                                  smb

                        Example:
                                http


                  > > Filter by Port Number


                        Syntax:

                                tcp.port == <port>
                                udp.port == <port>

                        Examples:

                                tcp.port == 80      (HTTP)
                                tcp.port == 443     (HTTPS)
                                udp.port == 53      (DNS)

                  > > Filter by IP Address

                        Syntax:
                                ip.addr == <IP Address>
                        Example:
                                ip.addr == 192.168.1.2
                        Displays all packets where the specified IP appears as either source or destination.


      ⭐ Importance of Display Filters :

                  > Reduce unnecessary traffic.
                  > Focus on events of interest.
                  > Speed up investigations.
                  > Simplify protocol analysis.
                  > Improve incident response and troubleshooting efficiency.

